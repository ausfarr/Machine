# Dashboard

Not yet implemented. Per CLAUDE.md, the dashboard is built in step 6,
after Scout, Loom, Bindery, and Crier exist — it needs real pipeline data
to render against and must never show fabricated/placeholder metrics.
